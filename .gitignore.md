# --- Operating System Files (Junk) ---
.DS_Store
Thumbs.db
Desktop.ini

# --- IDE & Editor Settings ---
# We ignore these because my editor settings might differ from yours
.vscode/
.idea/
*.swp

# --- Dependencies (NEVER commit these) ---
# Node.js
node_modules/
npm-debug.log
yarn-error.log

# Python
__pycache__/
*.pyc
venv/
env/
.pytest_cache/

# --- Security & Environment ---
# CRITICAL: Never commit keys, passwords, or database URLs
.env
.env.local
config.js
secrets.json

# --- Build Outputs ---
dist/
build/

# --- DATA & MEDIA STRATEGY ---
# Since we are scaling to huge data, we do NOT want raw data in Git.
# We keep templates (like concepts_template.json) but ignore the actual bulk data.
data/raw/
data/processed/
*.csv
*.sql
*.sqlite

# Ignore large media files (store these in S3 or a separate storage service later)
# Keep small assets for UI, but ignore bulk medical imagery
/assets/medical_images/
*.png
*.jpg
*.jpeg
# Exception: Allow specific UI images if needed (you can force add them)
!logo.png